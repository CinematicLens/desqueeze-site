App UI section uses original mockup images (not screenshots):

  import-export-ui.png   — Import & Export style UI mockup
  cinema-scope-export.png — Desqueeze Ratio & CinemaScope style UI mockup

These images were generated as original artwork. To use them on the live site,
copy the two PNG files from your Cursor project assets folder into this assets/
folder, then commit and push:

  From: .cursor\projects\...\assets\*.png
  To:   desqueeze-site\assets\

Same filenames: import-export-ui.png, cinema-scope-export.png.
